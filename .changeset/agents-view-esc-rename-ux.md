---
"@moonshot-ai/kimi-code": patch
---

Agents view: pressing Esc with no overlay open no longer quits the view (it was too easy to close by accident — use Ctrl+C twice or the exit command to leave; Esc still cancels a pending delete). Renaming a session now starts from a blank field so you type the new name directly instead of editing a pre-filled copy of the old title.
