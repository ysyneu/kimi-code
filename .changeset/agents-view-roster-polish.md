---
"@moonshot-ai/kimi-code": patch
---

Streamline the agents view roster: → opens a session just like Enter (the peek panel is gone); groups are separated by a blank line, reordered with Pinned first, and "Awaiting input" is renamed to "Needs input"; each row aligns into columns for its status symbol, name, and a summary of the session's latest assistant reply instead of its working directory; and the status symbol now distinguishes new output you haven't seen yet from output you've already seen.
