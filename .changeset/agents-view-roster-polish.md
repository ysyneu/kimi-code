---
"@moonshot-ai/kimi-code": patch
---

Streamline the agents view roster: → now opens a session just like Enter (the peek panel is gone), groups are separated by a blank line, and each row now shows a summary of the session's latest assistant reply instead of its working directory.
