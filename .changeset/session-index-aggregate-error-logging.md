---
"@moonshot-ai/kimi-code": patch
---

Include the per-shard causes in session-index reconciliation and mirror flush failure logs, so contention/corruption reports show the underlying reason instead of only the aggregate summary.
