---
"@moonshot-ai/kimi-code": patch
---

Fix several agents view issues found in daily use: the roster now lists only sessions the view itself created or attached (no more noise from other clients), the view no longer attaches to a stale registered server that cannot serve requests, → opens a session the same as Enter, working rows animate their spinner, and rows no longer show the session name twice.
