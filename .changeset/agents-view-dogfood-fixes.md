---
"@moonshot-ai/kimi-code": patch
---

Fix several agents view issues found in daily use: the roster now lists only sessions the view itself created or attached (no more noise from other clients), the view no longer attaches to a stale registered server that cannot serve requests, working rows animate their spinner, rows no longer show the session name twice, and a manually collapsed group header now shows how many sessions it's hiding.
