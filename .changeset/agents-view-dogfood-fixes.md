---
"@moonshot-ai/kimi-code": patch
---

Fix several agents view issues found in daily use: the roster now lists only sessions the view itself created or attached (no more noise from other clients), the view no longer attaches to a stale registered server that cannot serve requests, left/right arrows switch between the session list and the peek detail, working rows animate their spinner, and rows no longer show the session name twice.
