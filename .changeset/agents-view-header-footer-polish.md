---
"@moonshot-ai/kimi-code": patch
---

Polish the agents view's chrome: the header now shows the product name and version, the model new sessions will use, the working directory, and live status counts; the input box at the bottom uses a lighter rule-only frame; and the footer shows short, selection-aware key hints, with the rest of the shortcuts available via a `?` overlay.
