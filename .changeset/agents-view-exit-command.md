---
"@moonshot-ai/kimi-code": patch
---

Typing `exit` or `/exit` in the agents view's dispatch box and pressing Enter now closes the view, the same as Esc, instead of dispatching a new session with that text.
